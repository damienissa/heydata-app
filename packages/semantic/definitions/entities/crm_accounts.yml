name: crm_accounts
table: crm_accounts
description: CRM account records (stage, priority, tags, follow-up)
primaryKey: user_id
relationships:
  - target: user_profiles
    foreignKey: user_id
    targetKey: id
    type: one-to-one
    joinType: inner
owner: platform
tags:
  - crm
