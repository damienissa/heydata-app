name: user_subscriptions
table: user_subscriptions
description: User subscription and plan (Paddle, plan_type, status)
primaryKey: id
relationships:
  - target: user_profiles
    foreignKey: user_id
    targetKey: id
    type: one-to-one
    joinType: inner
owner: platform
tags:
  - core
  - billing
