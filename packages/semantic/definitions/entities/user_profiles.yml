name: user_profiles
table: user_profiles
description: User accounts and profile data
primaryKey: id
relationships:
  - target: user_subscriptions
    foreignKey: id
    targetKey: user_id
    type: one-to-one
    joinType: left
  - target: links
    foreignKey: id
    targetKey: user_id
    type: one-to-many
    joinType: left
  - target: crm_accounts
    foreignKey: id
    targetKey: user_id
    type: one-to-one
    joinType: left
  - target: click_logs
    foreignKey: id
    targetKey: user_id
    type: one-to-many
    joinType: left
  - target: app_installs
    foreignKey: id
    targetKey: owner_id
    type: one-to-many
    joinType: left
  - target: usage_tracking
    foreignKey: id
    targetKey: user_id
    type: one-to-many
    joinType: left
  - target: crm_emails
    foreignKey: id
    targetKey: user_id
    type: one-to-many
    joinType: left
  - target: crm_notes
    foreignKey: id
    targetKey: user_id
    type: one-to-many
    joinType: left
  - target: crm_tasks
    foreignKey: id
    targetKey: user_id
    type: one-to-many
    joinType: left
  - target: promocode_redemptions
    foreignKey: id
    targetKey: user_id
    type: one-to-many
    joinType: left
owner: platform
tags:
  - core
  - users
