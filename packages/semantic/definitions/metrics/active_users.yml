name: active_users
displayName: Active users
description: Count of distinct users (e.g. with links or clicks)
formula: COUNT(DISTINCT links.user_id)
grain: monthly
dimensions:
  - username
  - plan_type
  - subscription_status
synonyms:
  - users
  - user count
  - unique users
formatting:
  type: number
  decimalPlaces: 0
owner: platform
tags:
  - core
  - users
